interface:
  display_name: "Supervised Forge"
  short_description: "Single-author implementation with one correctness review gate"
  default_prompt: "Use $supervised-forge to implement this change milestone by milestone with persistent independent correctness review."
